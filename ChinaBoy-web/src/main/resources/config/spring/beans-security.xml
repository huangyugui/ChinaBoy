<b:beans xmlns="http://www.springframework.org/schema/security"
		 xmlns:b="http://www.springframework.org/schema/beans"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

	<!--登录页面不过滤 ,这种方式不会经过过滤器链-->
	<http pattern="/login.jsp" security="none" />

	<!-- auto-config="true" 会自动建立登录页面,基本认证，登出处理 -->
	<!-- use-expressions="true" 默认启用EL表达式,access="hasRole('ROLE_USER')"
		所以需要use-expressions="false" access="ROLE_USER"
	-->
	<http auto-config="true" use-expressions="false">
		<!-- 定义自定义登录页面以及登录url，以及用户名与密码参数名称 
			login-page="/login.jsp" 登录页面
			login-processing-url="/login.htm" 登录
			default-target-url="" 指定直接访问登录界面成功后的访问url，默认index首页
			authentication-success-handler-ref 指定授权成功后处理器,使default-target-url authentication-success-forward-url无效
			authentication-failure-handler-ref 指定授权失败后处理器,使authentication-failure-url authentication-failure-forward-url无效
		-->
		<form-login login-page="/login.jsp" login-processing-url="/login.htm" 
		username-parameter="j_username" password-parameter="j_password" 
		authentication-success-handler-ref="authenticationSuccessHandler"
		authentication-failure-handler-ref="authenticationFailureHandler"
		/>
		
		<!-- 启用spring security 弹窗式登录窗口 -->
		
		<!-- 匿名用户可以访问 -->
		<intercept-url pattern="/**" access="ROLE_USER"/>
	</http>
	
	<authentication-manager id="">
		<authentication-provider>
			<user-service>
				<user name="sky" password="sky" authorities="ROLE_USER" />
			</user-service>
		</authentication-provider>
	</authentication-manager>

</b:beans>